// JavaScript Documentvar current_fs, next_fs, previous_fs; //fieldsetsvar left, opacity, scale; //fieldset properties which we will animatevar animating; //flag to prevent quick multi-click glitchesvar fieldIndex = 0; //Field index is initiated to 0 increased for each "next"-buttom pressed and decreased for each "previous"-button pressedvar fieldIndexMap = 3; // Index with the mapvar mapLoaded; //Boolean which is true if the map has been loaded $('div[name=avgift-div]').hide(); // The avgift input is to be hid as default $(".next").click(function(){	if(animating) return false;	animating = true;			// Load the map when the correct field index has been reached	fieldIndex = fieldIndex + 1;	if(mapLoaded != true && fieldIndex == fieldIndexMap){		$("#pac-input").show();		$.getScript("https://maps.googleapis.com/maps/api/js?key=AIzaSyBWHxPyeMmIKG2h3_BaW3X3PngdwaLE2q8&libraries=visualization&libraries=places&callback=initMap");		mapLoaded = true;			}			if(fieldIndex == 2 && $( "#boTyp" ).val() == "Lägenhet"){		$("#yard-div").hide();	}else{		$("#yard-div").show();	}		current_fs = $(this).parent();	next_fs = $(this).parent().next();		//activate next step on progressbar using the index of next_fs	$("#progressbar li").eq($("fieldset").index(next_fs)).addClass("active");		//show the next fieldset	next_fs.show(); 		//hide the current fieldset with style	current_fs.animate({opacity: 0}, {		step: function(now, mx) {			//as the opacity of current_fs reduces to 0 - stored in "now"			//1. scale current_fs down to 80%			scale = 1 - (1 - now) * 0.2;			//2. bring next_fs from the right(50%)			left = (now * 50)+"%";			//3. increase opacity of next_fs to 1 as it moves in			opacity = 1 - now;			current_fs.css({'transform': 'scale('+scale+')'});			next_fs.css({'left': left, 'opacity': opacity});		}, 		duration: 0, 		complete: function(){			current_fs.hide();			animating = false;		}, 		//this comes from the custom easing plugin		easing: 'easeInOutBack'	});});$(".previous").click(function(){		if(animating) return false;	animating = true;	fieldIndex = fieldIndex - 1;		current_fs = $(this).parent();	previous_fs = $(this).parent().prev();		//de-activate current step on progressbar	$("#progressbar li").eq($("fieldset").index(current_fs)).removeClass("active");		//show the previous fieldset	previous_fs.show();	 	//hide the current fieldset with style	current_fs.animate({opacity: 0}, {		step: function(now, mx) {			//as the opacity of current_fs reduces to 0 - stored in "now"			//1. scale previous_fs from 80% to 100%			scale = 0.8 + (1 - now) * 0.2;			//2. take current_fs to the right(50%) - from 0%			left = ((1-now) * 50)+"%";			//3. increase opacity of previous_fs to 1 as it moves in			opacity = 1 - now;			current_fs.css({'left': left});			previous_fs.css({'transform': 'scale('+scale+')', 'opacity': opacity});		}, 		duration: 0, 		complete: function(){			current_fs.hide();			animating = false;		}, 		//this comes from the custom easing plugin		easing: 'easeInOutBack'	});});$(".submit").click(function(){	return false;})////////////////// Show rent div based on if lägenhet is choosen in the options $("#boTyp").on("click", function(){	if( $( "#boTyp" ).val() == "Lägenhet"){		$('div[name=avgift-div]').show();	} else {		$('div[name=avgift-div]').hide();	}});if( $( "#boTyp" ).val() == "Lägenhet"){		$('div[name=avgift-div]').show();	} else {		$('div[name=avgift-div]').hide();	}